{
  "config": {
    "step": {
      "user": {
        "title": "Jane Voice Assistant",
        "description": "Configure Jane — your Hebrew smart home voice assistant.",
        "data": {
          "gemini_api_key": "Gemini API Key"
        }
      }
    },
    "error": {
      "invalid_api_key": "Invalid Gemini API key. Please check and try again."
    }
  },
  "options": {
    "error": {
      "pg_connection_failed": "Cannot connect to PostgreSQL. Check host, port, and credentials."
    },
    "step": {
      "init": {
        "title": "Jane Settings",
        "data": {
          "firebase_key_path": "Firebase Key Path (optional)",
          "pg_host": "PostgreSQL Host (empty = use files)",
          "pg_port": "PostgreSQL Port",
          "pg_database": "PostgreSQL Database",
          "pg_user": "PostgreSQL User",
          "pg_password": "PostgreSQL Password",
          "redis_port": "Redis Port (default 6379, same host as PG)",
          "redis_password": "Redis Password (optional)"
        }
      }
    }
  }
}
